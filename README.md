# banking-system-java
A console-based banking system in pure Java — Account hierarchy, in-memory transaction history, and deposit/withdraw/transfer logic. Built solo as an OOP revision exercise (no Spring, no DB, no file I/O).
